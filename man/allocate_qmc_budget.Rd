% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/qmc_utils.R
\name{allocate_qmc_budget}
\alias{allocate_qmc_budget}
\title{Allocate a QMC Budget Across Triangles Proportionally to Area}
\usage{
allocate_qmc_budget(areas, n_total, min_area = 0)
}
\arguments{
\item{areas}{Numeric vector of triangle areas. Must be non-negative with no
\code{NA}s. Need not sum to any particular value; only relative
proportions are used.}

\item{n_total}{Positive integer. Requested total number of QMC points.
See the Budget behaviour section for when this may be exceeded.}

\item{min_area}{Non-negative numeric scalar. Triangles with area
\code{<= min_area} are treated as degenerate (e.g. floating-point
slivers from \code{st_intersection()}) and assigned zero points.
Default \code{0}, meaning only exactly-zero-area triangles are dropped.}
}
\value{
An integer vector of the same length as \code{areas}. Every entry
is non-negative. Entries for degenerate triangles (\code{area <= min_area})
are \code{0L}. If all triangles are degenerate, all entries are \code{0L}
and a warning is issued.
}
\description{
Given a vector of triangle areas, returns an integer vector of QMC point
counts. Every triangle with area above \code{min_area} is guaranteed at
least one point. The remaining budget is distributed proportionally to area
using the largest-remainder method.
}
\details{
This is a pure helper used by \code{\link[=map_qmc_to_triangulation]{map_qmc_to_triangulation()}}. It implements
proportional allocation as described in Cochran (1977, Ch. 5).
}
\section{Budget behaviour}{

The total points returned is \code{max(n_total, n_kept)}, where
\code{n_kept} is the number of triangles above \code{min_area}. When
\code{n_kept <= n_total} the total is exactly \code{n_total}. When
\code{n_kept > n_total} — more pieces than requested points — each piece
still gets one point and the total exceeds \code{n_total}; a message is
issued. This is preferable to silently giving some pieces zero coverage.
}

\examples{
# Two equal triangles split the budget evenly
allocate_qmc_budget(c(1, 1), n_total = 100)

# 101 equal pieces: each gets at least 1 point, remainder distributed
allocate_qmc_budget(rep(1, 101), n_total = 256)

# Degenerate zero-area sliver gets zero; all budget goes to valid piece
allocate_qmc_budget(c(1, 0), n_total = 100)

}
