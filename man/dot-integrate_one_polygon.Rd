% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/integrate.R
\name{.integrate_one_polygon}
\alias{.integrate_one_polygon}
\title{Integrate a Basis Function Over a Single Polygon}
\usage{
.integrate_one_polygon(
  polygon_sfc,
  basis_fn,
  k,
  strata_sf = NULL,
  qmc,
  min_area = 0
)
}
\arguments{
\item{polygon_sfc}{A length-1 \code{sfc} of type POLYGON or MULTIPOLYGON,
in a projected CRS.}

\item{basis_fn}{A function \code{function(coords)} where \code{coords} is
an \eqn{n \times 2} numeric matrix of (x, y) coordinates in the same CRS
as \code{polygon_sfc}. Must return an \eqn{n \times k} numeric matrix.}

\item{k}{Integer. Number of basis functions (columns returned by
\code{basis_fn}). Typically inferred by \code{\link[=integrate_basis]{integrate_basis()}}.}

\item{strata_sf}{Optional \code{sf} object of strata polygons. If supplied,
the polygon is intersected with each stratum before triangulating, which
guarantees QMC points never straddle a stratum boundary. Must be in the
same CRS as \code{polygon_sfc}.}

\item{qmc}{An \eqn{n \times 2} matrix of QMC points in \eqn{[0,1]^2},
from \code{\link[=generate_qmc_unit_square]{generate_qmc_unit_square()}}. Generated once and shared across all
polygons.}

\item{min_area}{Non-negative numeric. Triangles with area \code{<= min_area}
are skipped. Default \code{0}.}
}
\value{
A length-k numeric vector. Returns a vector of \code{NA_real_} if
the polygon produces no valid triangles (e.g. zero-area input).
}
\description{
Computes one row of the integration matrix by evaluating \code{basis_fn}
over a single polygon, optionally intersecting with strata first. Returns
a length-k numeric vector — the area-weighted average of \code{basis_fn}
over the polygon.
}
\details{
This is the per-polygon worker called by \code{\link[=integrate_basis]{integrate_basis()}}. It can also
be called directly for debugging or custom parallelism.
}
\seealso{
\code{\link[=integrate_basis]{integrate_basis()}}, \code{\link[=generate_qmc_unit_square]{generate_qmc_unit_square()}}
}
