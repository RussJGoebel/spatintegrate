% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fourier_prior.R, R/sar_matern_matching.R
\name{get_matern_parameters_from_SAR}
\alias{get_matern_parameters_from_SAR}
\title{Convert SAR parameters to matched Matern parameters (unit-square frame)}
\usage{
get_matern_parameters_from_SAR(rho, sar_precision, h, d = 2)

get_matern_parameters_from_SAR(rho, sar_precision, h, d = 2)
}
\arguments{
\item{rho}{SAR autocorrelation in (0,1).}

\item{sar_precision}{SAR prior precision tau (= 1/sigma2_SAR).}

\item{h}{Normalised pixel size = 1/n_cells_side.}

\item{d}{Dimension (default 2).}
}
\value{
Named list: matern_precision (TAU), kappa2 (in \link{0,1}^2 units).

Named list: matern_precision (TAU), kappa2 (unit-square units),
sar_variance, matern_variance.
}
\description{
All quantities live in the normalised \link{0,1}^2 domain.
Use h = 1/n_cells_side (normalised pixel size).

All quantities in the normalised \link{0,1}^2 domain.
Use h = 1/n_cells_side (normalised pixel size).
}
\details{
Matching formulas (rook adjacency, d=2):
kappa2_norm     = 4*(1-rho) / (rho * h_norm^2)
matern_variance = sigma2_SAR * (2d/(rho*h_norm))^2
TAU             = 1 / matern_variance

kappa2_norm relates to physical kappa2 by: kappa2_phys = kappa2_norm / L^2
where L = max(Lx, Ly) is the square embedding side length.
}
\seealso{
fourier_matern_Q_sb, sar_to_matern
}
