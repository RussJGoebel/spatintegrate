% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/qmc_utils.R
\name{map_qmc_to_triangulation}
\alias{map_qmc_to_triangulation}
\title{Map a QMC Budget Across a Triangulation}
\usage{
map_qmc_to_triangulation(triangles, n_total, min_area = 0)
}
\arguments{
\item{triangles}{An \code{sfc} object of POLYGON geometries, each a
triangle (exactly 3 unique exterior vertices). Typically the output of
\code{\link[=triangulate_sf]{triangulate_sf()}}.}

\item{n_total}{Positive integer. Total QMC points to distribute across all
triangles. May be exceeded if the number of non-degenerate triangles
exceeds \code{n_total}; see \code{\link[=allocate_qmc_budget]{allocate_qmc_budget()}}.}

\item{min_area}{Non-negative numeric scalar passed to \code{\link[=allocate_qmc_budget]{allocate_qmc_budget()}}.
Triangles with area \code{<= min_area} are treated as degenerate and
skipped. Default \code{0}.}
}
\value{
A list with two elements:
\describe{
\item{\code{coords}}{An \eqn{m \times 2} numeric matrix of mapped
coordinates, where \eqn{m \leq} \code{n_total}.}
\item{\code{labels}}{An integer vector of length \eqn{m}. \code{labels[k]}
gives the index (into \code{triangles}) of the triangle that point
\code{k} belongs to.}
}

If all triangles are dropped by the epsilon threshold, returns
\code{list(coords = matrix(nrow=0, ncol=2), labels = integer(0))}.
}
\description{
Given an \code{sfc} collection of triangles and a total QMC budget
\code{n_total}, generates Sobol points inside each triangle proportionally
to its area. Returns the mapped coordinates and a label vector indicating
which triangle each point belongs to.
}
\details{
This is the key Layer 1 primitive. It replaces the pattern of calling
\code{\link[=map_unit_square_to_triangle]{map_unit_square_to_triangle()}} separately per triangle with a single call
that batches all triangles together — enabling \code{\link[=integrate_one_sounding]{integrate_one_sounding()}}
to make one \code{basis_fn()} call for all points simultaneously.
}
\examples{
\dontrun{
tris <- triangulate_sf(some_polygon_sf)
result <- map_qmc_to_triangulation(tris, n_total = 256)
nrow(result$coords)   # <= 256
range(result$labels)  # subset of 1:length(tris)
}

}
