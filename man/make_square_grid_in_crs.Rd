% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/make_square_grid_in_crs.R
\name{make_square_grid_in_crs}
\alias{make_square_grid_in_crs}
\title{Square grid over (buffered) geometry, preserving the input CRS}
\usage{
make_square_grid_in_crs(
  x,
  res,
  buffer = 0,
  shape = c("rectangle", "hull"),
  align_origin = NULL,
  clip = TRUE
)
}
\arguments{
\item{x}{An \code{sf} or \code{sfc} geometry with a CRS.}

\item{res}{Positive numeric scalar. Square cell size in \strong{CRS units}
(degrees for lon/lat; meters/feet for projected).}

\item{buffer}{Nonnegative numeric scalar. Buffer distance applied to the masking
geometry (rectangle or hull) in \strong{CRS units}. Default \code{0}.}

\item{shape}{One of \code{"rectangle"} (buffered bbox; fastest) or \code{"hull"} (buffered convex hull).}

\item{align_origin}{Optional numeric length-2 \verb{(x0, y0)} to align the grid lattice.
If \code{NULL}, the grid anchors at the (buffered) extent's lower-left corner.}

\item{clip}{Logical. If \code{TRUE} and \code{shape = "hull"}, drop cells outside the buffered hull.}
}
\value{
An \code{sf} polygon grid with columns:
\itemize{
\item \code{row}, \code{col} — 1-based lattice indices (stable under clipping).
\item Attributes: \code{cellsize}, \code{origin}, \code{buffer}, \code{shape}, \code{crs_used}.
}
}
\description{
Create a regular grid of \strong{square} polygons with cell size \code{res} over either a
buffered bounding rectangle or a buffered convex hull of \code{x}. The function does
\strong{not} reproject: all distances are interpreted in the units of \code{st_crs(x)}.
If \code{x} is geographic (lon/lat), \code{res} is in \strong{degrees}.
}
\note{
On geographic CRS with \code{sf::sf_use_s2()} TRUE, \code{st_buffer()} is geodesic (meters)
while \code{res} is in degrees. Either disable s2 (\code{sf::sf_use_s2(FALSE)}), set \code{buffer = 0},
or reproject beforehand if you need metric consistency.
}
\examples{
\dontrun{
g <- make_square_grid_in_crs(my_geom, res = 330, buffer = 0, shape = "rectangle")
g2 <- make_square_grid_in_crs(my_geom, res = 250, buffer = 500,
                              shape = "hull", align_origin = c(0, 0))
}
}
