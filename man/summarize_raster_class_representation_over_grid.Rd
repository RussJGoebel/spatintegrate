% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/raster_summaries.R
\name{summarize_raster_class_representation_over_grid}
\alias{summarize_raster_class_representation_over_grid}
\title{Summarize a categorical SpatRaster over an sf grid}
\usage{
summarize_raster_class_representation_over_grid(SpatRaster_object, sf_grid)
}
\arguments{
\item{SpatRaster_object}{A single-layer terra SpatRaster with integer/categorical values.}

\item{sf_grid}{An sf POLYGON object representing the target grid. Each row is one cell.}
}
\value{
\code{sf_grid} with additional columns:
\itemize{
\item \verb{n_<label>} — count of fine-resolution pixels of each class within the cell.
\item \verb{proportion_<label>} — proportion of pixels of each class within the cell.
\item \code{dominant_class} — the class with the highest proportion in each cell.
\item \code{pixel} — integer index identifying each grid cell (1-based row index).
}
Grid cells outside the raster extent are retained with NA landcover values.
Original row order is preserved.
}
\description{
For each cell in an sf grid, computes the count and proportion of fine-resolution
raster pixels belonging to each category, plus the dominant (modal) class.
Works by rasterizing the sf grid onto the SpatRaster's own grid, then
tabulating category membership — which is efficient when the raster is at
much finer resolution than the grid (e.g., 10m landcover over a 330m grid).
}
\details{
Column names use the raw raster values as labels (e.g., \code{n_1}, \code{proportion_6}).
Use \code{\link{rename_and_fill_proportions}} afterward to map numeric codes
to human-readable names, and to fill implicit NAs with 0.
}
\examples{
\dontrun{
grid <- summarize_raster_class_representation_over_grid(landcover_rast, target_grid)
}

}
