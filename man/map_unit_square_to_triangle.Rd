% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/qmc_utils.R
\name{map_unit_square_to_triangle}
\alias{map_unit_square_to_triangle}
\title{Map Unit Square Points to a Triangle}
\usage{
map_unit_square_to_triangle(qmc_points, triangle_coords)
}
\arguments{
\item{qmc_points}{An \eqn{n \times 2} numeric matrix of points in
\eqn{[0,1]^2}.}

\item{triangle_coords}{A \eqn{3 \times 2} numeric matrix of triangle vertex
coordinates \eqn{(x, y)}, one vertex per row.}
}
\value{
An \eqn{n \times 2} numeric matrix of mapped coordinates. All
returned points lie inside (or on the boundary of) the triangle.
}
\description{
Maps an \eqn{n \times 2} matrix of points from \eqn{[0,1]^2} into a
triangle defined by three vertex coordinates, using a uniform barycentric
transformation. Points outside the standard simplex (\eqn{u + v > 1}) are
reflected so that the mapping is area-preserving and bijective.
}
\examples{
tri <- matrix(c(0,0, 1,0, 0,1), ncol = 2, byrow = TRUE)
pts <- generate_qmc_unit_square(64)
mapped <- map_unit_square_to_triangle(pts, tri)
# All points inside triangle: u + v <= 1
stopifnot(all(rowSums(mapped) <= 1 + 1e-10))

}
